python files dependency analysis:
  for "import local_python_module" statements
  for dependency on other files with open(filename) or other functions.
  for import of external dependencies and generate external depenencies.

- when doing batch processing we are not using multi core. What can we do about it?

- we need to cache the output of mass generators better. We didnt handle it up till now.
    lets take mkdocs in veltzer.github.io. It generates a whole folder and we need ot cache that folder
    and be honest about it.
    The abstraction:
    In checkers there is no output.
    In geneators the outputs are derived from the inpput (one to one)
    in mass geneatros which really should be called another name (creators) since they can create either:
      - a single files
      - several files
      - a whole folder
      - several folders
      - or any combination of the above
    And we should really know how to cache their outputs.

- want rsconstruct processors config [TAB] to complete inames
- want rsconstruct processors defconfig [TAB] to complete pnames

- do a command "rsconstruct errors" which prints name of errors and their error code (BUILD_ERROR=1 etc)

- do we have a test that checks that we get an error if we have two processors in the same tmpl file without differentiating between then with a different name.

- do "rsconstruct processors list" and you will see that many processors do not have descriptions. A desciption should be printed only with the --verbose flag.

- rsconstruct processors types should print out a table. --json will emit json output.

- explciit processor command should be none and a must field.

- rsconstruct processors defconfig explicit should print only the table.
    description should be printed only under --verbose.
    I want to see other columns like must etc.


- make "rsconstruct deps list" --json emit json

- "rsconstruct deps used" still uses color. Have a global color flag and turn it off for now because it is causing problems with tables.

- what is ScanConfig and why do we have it?
